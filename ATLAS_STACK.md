# Atlas Stack Status
Generated by scaffolder. Read this before writing any code.

## Verdict: EXISTING — preserve current stack

## Detected stack
- Runtime: NestJS (TypeScript) + Angular (TypeScript)
- ORM: Prisma
- Auth: JWT (passport-jwt strategy, Bearer header)
- Health checks: present at /api/health/live and /api/health/ready
- K8s service name: none detected

## Rules for all agents
- Do not migrate the ORM
- Do not replace the auth implementation
- Do not change the framework
- Only add features within the existing patterns
- Frontend lives in `frontend/` (Angular standalone components), backend in `backend/` (NestJS)
- All backend routes use global `/api` prefix
- Do not scaffold a separate `web/` subdirectory — the Angular frontend IS the web platform at `frontend/`
